package com.example.multimediaapp.viewmodel.events

/**
 * Sealed class que representa todos los eventos posibles en la pantalla de registro.
 */
/*sealed class RegisterEvent {

    // Evento para registrar un usuario
    data class RegisterUser(val user: UsersDTO) : RegisterEvent()

    // Evento cuando el usuario cambia el campo "username"
    data class OnUserChange(val user: String) : RegisterEvent()

    // Evento cuando el usuario cambia el campo "email"
    data class OnEmailChange(val email: String) : RegisterEvent()

    // Evento cuando el usuario cambia el campo "password"
    data class OnPassChange(val pass: String) : RegisterEvent()

    // Evento opcional para mostrar un mensaje de error
    data class ShowError(val message: String) : RegisterEvent()
}

 */