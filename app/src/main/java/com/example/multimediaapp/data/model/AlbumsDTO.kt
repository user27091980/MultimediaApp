package com.example.multimediaapp.data.model

//si queremos simular API (url) introducimos String
//si lo queremos en memoria como entero y añadimos al repo para ambos casos
data class AlbumsDTO(

    var id: Int,
    var imageAlbums: String,

    )